# === AI Service Environment ===
GEN_MODEL=llama3   # or: solar

# Embedding backend: ollama | openai
EMBEDDING_BACKEND=ollama

# Embedding model (per backend):
# - If EMBEDDING_BACKEND=ollama → use an ollama embedding model (e.g. nomic-embed-text, mxbai-embed-large)
# - If EMBEDDING_BACKEND=openai → OpenAI model name (e.g. text-embedding-3-small)
EMBEDDING_MODEL=nomic-embed-text

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
VECTOR_DIM=768  # dim must match EMBEDDING_MODEL

# OpenAI (only if using EMBEDDING_BACKEND=openai)
OPENAI_API_KEY=